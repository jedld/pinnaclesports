require 'spec_helper'

describe Pinnaclesports do
  it 'has a version number' do
    expect(Pinnaclesports::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
